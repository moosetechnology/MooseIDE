Extension { #name : #MiCBContextBuilderPresenter }

{ #category : #'*MooseIDE-CriticBrowser-Tests' }
MiCBContextBuilderPresenter >> contextSummary [
"used for testing purposes, do not access outside of tests"
	^ summaryField
]

{ #category : #'*MooseIDE-CriticBrowser-Tests' }
MiCBContextBuilderPresenter >> contextTitle [
"used for testing purposes, do not access outside of tests"
	^ titleField
]
